/**
 * Add Score Intro Modal
 * Stub: UI moved to sheet or removed. Kept for type-safe imports.
 */

import React from 'react';

interface AddScoreIntroModalProps {
  visible?: boolean;
  onClose?: () => void;
  onAddScore?: () => void;
  onNeverShowAgain?: () => void;
}

export function AddScoreIntroModal(_props: AddScoreIntroModalProps) {
  return null;
}
